name: Docker Base Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  BASE: ghcr.io/lgisler/blue-whale/base
  XCBASE: ghcr.io/lgisler/blue-whale/xc-base-arm64

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Log into docker registry
      run: echo "${{ secrets.DOCKER_TOKEN  }}" | docker login ghcr.io/lgisler -u "${{ secrets.DOCKER_USER  }}" --password-stdin

    - name: Configure files
      run: ./configure

    - name: Run binfmt image
      run: docker run --privileged --rm docker/binfmt:a7996909642ee92942dcd6cff44b9b95f08dad64

    - name: Build base image
      run: |
        docker buildx create --use
        docker buildx build \
          --output type=image,name=$BASE,push=true \
          --cache-to type=registry,ref=$BASE:buildcache,push=true \
          --cache-from type=registry,ref=$BASE:buildcache \
          native-compile/base/.

    - name: Build cross compile base image
      run: |
        docker buildx create --use
        docker buildx build \
          --output type=image,name=$XCBASE,push=true \
          --cache-to type=registry,ref=$XCBASE:buildcache,push=true \
          --cache-from type=registry,ref=$XCBASE:buildcache \
          cross-compile/aarch64-linux-gnu/xc-base/.
