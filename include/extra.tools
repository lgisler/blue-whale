# file: building-blocks/extra.tools
#
# extra tools used by developers
#

RUN apt-get update && \
    apt-get --assume-yes install \
    gdb \
    vim \
    bash-completion \
    sudo && \
    apt-get clean --yes
